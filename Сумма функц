#include <iostream>
#include <cmath>

// Создать функцию sin(x)
двойная функция(двойная x, целая n) {
двойная сумма_f = 0,0;
двойной q = x;

для (int i = 1; i < n; ++i) {
сумма_f += q;
двойной p = (двойной)((2,0 * i) * (2,0 * i + 1));
q *= pow(-1.0, (2 * x + 1)) / p;
}
вернуть сумму_f;
}

инт мейн() {
// узлы драйвера
// Введите значение в градусах по оси x
двойной икс;
std::cout << "Введите значение в градусах x = ";
стд::cin >> х;

// Введите количество терминов
целочисленное n;
std::cout << "Введите количество терминов n = ";
стд::cin >> n;

// вызов функции
двойной s = функция(x, n);

std::cout << "сумма = " << s << std::endl;

возврат 0;
}
